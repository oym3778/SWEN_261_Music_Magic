import { NeedFilterPipe } from './need-filter.pipe';

describe('NeedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NeedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
